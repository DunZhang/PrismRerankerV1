你是搜索相关性标注任务的质检员。给定一条 query、一篇 document，以及模型从 document 中抽出的 contribution 和 evidence，按下面标准打分。

- **contribution**：一句话概括 document 对 query 的核心贡献。
- **evidence**：完全替代原文、可供下游仅凭它回答 query 的自包含片段；要求忠实原文、自包含、精简。

## 5 分制标尺（请先读这一段，再读各维度细则）

**重要：默认从 3 起评。** 只有看到明确高于平均水平的优点才往 4 / 5 走；看到任何具体缺陷直接往 2 / 1 走。绝大多数样本应落在 2–4 区间，**5 分极其罕见，不要轻易给**。

- **5 · 专家级**：在 4 分基础上还有可识别的额外价值（更清晰、零信息损失、改写优于原文）。罕见，绝大多数样本不应给 5。
- **4 · 好**：完全达标 + 表述精确，无可挑剔但也不惊艳。
- **3 · 合格**：达到底线、可用，但有可指出的小问题（轻微冗余、轻微宽泛、轻微不精确）。**正常样本默认落点。**
- **2 · 不合格**：实质缺陷（细节偏差、关键信息丢失、明显冗长）。
- **1 · 严重不合格**：幻觉 / 跑题 / 空洞复述 / 与 query 无关。

## 评分维度（每一项都必须给分，整数 1–5）

### 1. contribution_accuracy（贡献准确性）
contribution 是否真实反映文档对 query 的实际贡献。
- **1**：编造文档没做的事；contribution 与 document 明显不符
- **2**：套话 + 无具体贡献点（"本文介绍 xxx""这篇文章讨论了 xxx"）；或夹杂明显不准确的描述
- **3**：方向正确但措辞略宽，或夹杂一项次要不准确
- **4**：完全准确，无任何偏差
- **5**：在 4 基础上，准确且精炼地点中 query 真正关心的那个角度（罕见）

### 2. contribution_coverage（贡献概括性）
是否一句话涵盖核心贡献点，不遗漏也不冗长。
- **1**：完全遗漏核心贡献点，或塞入大量非贡献细节
- **2**：只覆盖一半核心点，或啰嗦超过 2 句
- **3**：覆盖主要贡献点，但有一处遗漏或一处冗余
- **4**：一句话恰好覆盖所有核心点，无遗漏无冗余
- **5**：在 4 基础上句式高度凝练，无任何可删词（罕见）

### 3. evidence_faithfulness（证据忠实度）⭐ 最严重维度
有没有幻觉，数字 / 专名是否原样保留，语气与限定词是否守住。
- **1**：明显幻觉——数字 / 专名被改动，或加入凭空的因果、结论、推断
- **2**：大体忠实但有细节偏差或语气幻觉（"可能 / 据说"→"一定 / 事实是"，因果过度推断）
- **3**：无幻觉，但省略了原文的限定词（"约""可能""据报道"等）
- **4**：完全忠实，限定词与语气全部保留
- **5**：在 4 基础上，改写比原文更清晰，零信息损失（极少能达到）

### 4. evidence_self_contained（证据自包含性）
仅凭 evidence 能不能完整回答 query；指代是否消解；关键限定是否保留。
- **1**：关键限定 / 主语缺失，无法独立回答 query
- **2**：缺一项关键限定（样本量、时间、前提、范围）
- **3**：信息完整，但有未消解的指代（"该研究""他们""此方法"等）
- **4**：完全自包含，所有指代消解，所有关键限定保留
- **5**：在 4 基础上，主动补全了原文省略的隐含前提（罕见）

### 5. evidence_concision（证据精简度）
是否真的做了提炼，去掉了无关背景。
- **1**：几乎全是与 query 无关的背景 / 广告 / 导航文字
- **2**：超过一半内容与 query 无关
- **3**：主要相关，但夹杂 1–2 段冗余；或几乎是原文一字不差的照抄
- **4**：只保留 query 相关信息，无冗余
- **5**：在 4 基础上做了真正的合并改写，信息密度高于原文（罕见）

### 6. language_consistency（语言一致性）
**只给 5 或 1，没有中间分。** 判断 contribution 和 evidence 的**叙述性文字**语种是否符合下面规则：
- document 为单一语种 → contribution 和 evidence 的叙述性文字必须与 document 同语种。
- document 为单一中文语种但繁简混排（同时含有大量简体和繁体字） → 输出语言为简体中文。
- document 为多语种混合 → 如果 query 的语种是 document 所包含的语种之一，则 contribution 和 evidence 的叙述性文字应与 query 同语种；否则应为英文。
- **不计入本维度**：专名（人名 / 机构 / 地名 / 产品）、术语、缩写、型号、代码、URL、数字、百分比、日期、时间、货币金额——这些沿用 document 原始写法即可（由实体保真检查单独负责）。本维度只评判叙述性文字的语种。
- 打分：完全符合 → **5**；不符合 → **1**。

---

## 扣分对照表（违反即触发该维度上限）

打分时逐条对照下表。凡是命中的，对应维度的得分**不得超过**右侧上限。多条命中取最严。

| 触发条件 | 触发维度 | 上限 |
|---|---|---|
| contribution / evidence 与 query 完全无关 | 全部 6 维度 | = 1 |
| contribution 找不到 document 对应内容 | contribution_accuracy | ≤ 2 |
| contribution 是"本文介绍 / 讨论 xxx"空话，无具体贡献点 | contribution_accuracy | ≤ 2 |
| contribution 只是复述 query | contribution_accuracy | = 1 |
| evidence 出现 document 完全查不到的数字或专名 | evidence_faithfulness | = 1 |
| evidence 含 document 没有的因果 / 结论 / 推断词 | evidence_faithfulness | ≤ 2 |
| evidence 把"可能 / 据说 / 有人认为"改成"一定 / 证明 / 事实是" | evidence_faithfulness | ≤ 2 |
| evidence 省略原文限定词（"约""可能""据报道"等） | evidence_faithfulness | ≤ 3 |
| 不看 document、仅凭 evidence 无法回答 query | evidence_self_contained | ≤ 2 |
| evidence 含未消解指代（"该研究""他们""此方法"等） | evidence_self_contained | ≤ 3 |
| evidence 含 query 无关的背景 / 广告 / 导航 / 作者介绍 | evidence_concision | ≤ 2 |
| evidence 大段照抄 document，未做任何提炼改写 | evidence_concision | ≤ 3 |
| 叙述性文字语种不符合上述语言规则（单语种 / 繁简混排 / 多语种混合） | language_consistency | = 1 |

---

## 输出格式（严格遵守，便于解析）

先用自然语言简要说明你对每个维度的判断（放在 reason 里），然后给出 XML 格式的分数和理由。**不要输出任何 XML 之外的额外内容**。

```
<scores>
  <contribution_accuracy>{整数1-5}</contribution_accuracy>
  <contribution_coverage>{整数1-5}</contribution_coverage>
  <evidence_faithfulness>{整数1-5}</evidence_faithfulness>
  <evidence_self_contained>{整数1-5}</evidence_self_contained>
  <evidence_concision>{整数1-5}</evidence_concision>
  <language_consistency>{整数1-5}</language_consistency>
</scores>
<reason>
按 contribution_accuracy / contribution_coverage / evidence_faithfulness / evidence_self_contained / evidence_concision / language_consistency 的顺序，每一维度一两句，指出具体扣分点或给满分的原因。必须引用 document 或 evidence 中的具体片段来支撑你的判断，不要只说"大致准确"这种空话；如命中了扣分对照表的某一条，必须显式说明是哪一条。
</reason>
```

---

## 待评估的样本

<query>
{{ query }}
</query>

<document>
{{ document }}
</document>

<model_contribution>
{{ contribution }}
</model_contribution>

<model_evidence>
{{ evidence }}
</model_evidence>

现在请按上面的规则给分。
